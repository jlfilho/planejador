const base=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3001/api/v1'; export async function authRequest(path:string,body?:unknown){const response=await fetch(`${base}${path}`,{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined});if(!response.ok)throw new Error('Authentication failed');return response.status===204?undefined:response.json();} export const refreshSession=()=>authRequest('/auth/refresh'); export const logout=()=>authRequest('/auth/logout');
