'use client'; import { useState } from 'react'; import { authRequest, logout, refreshSession } from '../lib/auth'; export default function Home(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(path:string){try{await authRequest(path,{email,password});setMessage('Sessão iniciada.');}catch{setMessage('Não foi possível autenticar.');}}return <main><h1>Planejador BNCC</h1><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><button onClick={()=>submit('/auth/login')}>Entrar</button><button onClick={()=>submit('/auth/register')}>Criar conta</button><button onClick={()=>refreshSession().then(()=>setMessage('Sessão renovada.')).catch(()=>setMessage('Nova autenticação necessária.'))}>Renovar sessão</button><button onClick={()=>logout().then(()=>setMessage('Sessão encerrada.'))}>Sair</button><p>{message}</p></main>}
