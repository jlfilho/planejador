import 'reflect-metadata'; import { NestFactory } from '@nestjs/core'; import { AppModule } from '../app.module'; import { UsersService } from '../users/users.service'; async function run(){const email=process.env.ADMIN_BOOTSTRAP_EMAIL;const password=process.env.ADMIN_BOOTSTRAP_PASSWORD;if(!email||!password)throw new Error('ADMIN_BOOTSTRAP_EMAIL and ADMIN_BOOTSTRAP_PASSWORD are required');const app=await NestFactory.createApplicationContext(AppModule,{logger:['error','warn']});try{const users=app.get(UsersService);const admin=await users.bootstrap(email,password);console.log(`Created ADMIN ${admin.id}`);}finally{await app.close();}} void run();
